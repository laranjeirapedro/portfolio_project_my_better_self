import { colors } from "@app/styles";
import styled from "@emotion/styled";

export const NavbarContainer = styled.div`
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  padding: 20px;
  background-color: ${colors.darkGrey[500]};
  width: 100%;
  min-height: 50px;
`;